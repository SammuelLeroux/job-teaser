<?php
require_once 'models/PersonneModel.php';

//class AuthenticatorController {
    //public function authentifier($nomUtilisateur, $motDePasse) {
        //$personneModel = new getAuthenticator();

        // Vous pouvez ajouter ici la logique pour gérer le nombre de tentatives, par exemple en utilisant des sessions
        // Si le nombre de tentatives dépasse un certain seuil, bloquez l'utilisateur pendant un certain temps

        //if ($personneModel->verifierAuthentification($nomUtilisateur, $motDePasse)) {
            // Authentification réussie, connectez l'utilisateur
            // Vous pouvez utiliser des sessions pour gérer la connexion de l'utilisateur
            //$_SESSION['nom_utilisateur'] = $nomUtilisateur;
            //return true;
        //}

        // Authentification échouée
        //return false;
    //}
//}
?>
